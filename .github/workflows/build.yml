name: Build Windows Electron App

on:
  push:
    branches: [main]

jobs:
  build-windows:
    runs-on: windows-latest
    env:
      GH_TOKEN: ${{ secrets.GH_TOKEN }}  # 用于 electron-builder 发布（可选）

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Install dependencies
        run: npm ci

      - name: Build frontend
        run: npm run build

      - name: Build Windows exe
        run: npm run dist:win

      - name: Upload Windows build artifact
        uses: actions/upload-artifact@v3
        with:
          name: phoneinfo-win
          path: dist/win-unpacked/**   # 打包输出目录
